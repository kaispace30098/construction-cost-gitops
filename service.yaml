apiVersion: v1
kind: Service
metadata:
  name: construction-cost-model
spec:
  selector:
    app: construction-cost-model
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP